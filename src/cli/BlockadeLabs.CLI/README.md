# BlockadeLabs.CLI

Command-line interface for the BlockadeLabs SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global BlockadeLabs.CLI --prerelease
```

## Usage

```bash
blockade-labs --help
blockade-labs api --help
```